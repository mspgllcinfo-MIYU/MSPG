<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive Icon background: solid black, matching the card-style
     background baked into the provided ic_launcher_foreground artwork
     (sampled from its own corners) so the safe-zone padding around the
     inset foreground blends seamlessly instead of showing a visible
     seam or mismatched tone. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M0,0 H108 V108 H0 Z"
        android:fillColor="#000000" />
</vector>
